[package]
name = "solana-kyc-verifier"
version = "0.1.0"
edition = "2026"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
solana-program = "1.16"
borsh = "0.10"
borsh-derive = "0.10"
# For verification, we need a curve; we'll use solana_program's built-in ed25519.
# No extra dependencies.